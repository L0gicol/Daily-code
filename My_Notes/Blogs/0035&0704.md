---

## 1. LeetCode 704. 二分查找 (Binary Search)

### 📝 题目描述
给定一个 `n` 个元素有序的（升序）整型数组 `nums` 和一个目标值 `target`  ，写一个函数搜索 `nums` 中的 `target`，如果目标值存在返回下标，否则返回 `-1`。

### 💡 核心逻辑：标准模板
我们要维护一个闭区间 `[left, right]`：
1.  **循环条件**：`while (left <= right)`。因为当 `left == right` 时，区间里还有一个元素，必须检查。
2.  **防止溢出**：计算 `mid` 时，使用 `left + (right - left) / 2`，而不是 `(left + right) / 2`。
3.  **区间收缩**：
    * `target < nums[mid]`：目标在左边，`right = mid - 1`。
    * `target > nums[mid]`：目标在右边，`left = mid + 1`。

### 💻 C++ 代码实现

```cpp
class Solution {
public:
    int search(vector<int>& nums, int target) {
        // 1. 定义左右边界（左闭右闭区间）
        int left = 0;
        int right = nums.size() - 1; 
        
        // 2. 只要左边还没跑到右边后面去，就继续找
        while(left <= right){
            // 3. 防溢出的中点计算
            int mid = left + (right - left) / 2;
            
            if(nums[mid] == target) {
                return mid; // 找到了！
            } 
            else if(target < nums[mid]) {
                // 中间值比目标大，说明在左边，right 要缩回来
                right = mid - 1;
            } 
            else {
                // 中间值比目标小，说明在右边，left 要迈过去
                left = mid + 1;
            }
        }
        
        // 4. 找了一圈没找到
        return -1;
    }
};
```

---

## 2. LeetCode 35. 搜索插入位置 (Search Insert Position)

### 📝 题目描述
在排序数组中查找目标值。如果不存在，返回它将会被按顺序插入的位置。

### 💡 解题思路
这道题和 704 几乎一模一样，唯一的区别是 **“没找到”时的返回值**。
* 在标准二分模板中，当循环结束时，`left` 的位置恰好就是 **第一个大于 target 的元素位置**。
* 这个位置，也就是 `target` 应该被插入的位置。

### 💻 C++ 代码实现
直接复用上面的模板，只改动最后一行 `return`。

```cpp
class Solution {
public:
    int searchInsert(vector<int>& nums, int target) {
        int left = 0;
        int right = nums.size() - 1; 
        
        while(left <= right){
            int mid = left + (right - left) / 2;
            
            if(nums[mid] == target) return mid;
            else if(target < nums[mid]) right = mid - 1;
            else left = mid + 1;
        }
        
        // 循环结束时，left 就是插入位置
        return left;
    }
};
```

### 🧠 总结
二分查找并不需要死记硬背各种版本（什么左开右闭、左闭右开），只要熟练掌握这一套 **`left <= right`** 的模板，足以应付绝大多数基础场景。