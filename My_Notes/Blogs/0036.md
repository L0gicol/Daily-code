# 📅 Day 6 (Part 2)：二分查找进阶——寻找左右边界

做完了基础题，遇到了一道 **Medium** 难度的题目：**LeetCode 34. 在排序数组中查找元素的第一个和最后一个位置**。

---

## 1. 题目难点与陷阱

**题目要求**：在排序数组中找出 `target` 的开始和结束位置。如果不存在返回 `[-1, -1]`。**必须设计 $O(\log n)$ 的算法。**

### ❌ 陷阱：线性向外扩散
初学者最容易想到的做法是：先用二分找到一个 `target`，然后用 `while` 循环向左 `left--`、向右 `right++` 去找边界。

* **问题**：如果数组里有 100 万个 8，这种做法会退化成 $O(n)$，导致超时 (TLE)。题目明确要求 $O(\log n)$。

---

## 2. 核心思路：Candidate (候选人) 策略

要实现真正的 $O(\log n)$，我们需要做 **两次二分查找**：
1.  一次专门找 **左边界**。
2.  一次专门找 **右边界**。

### “候选人”思想
当 `nums[mid] == target` 时，我们**不能直接 return**，因为这不一定是边界。
* **找左边界时**：发现了 `target`，先把它记录为 **candidate (暂定解)**，然后**继续向左收缩** (`right = mid - 1`)，看看左边还有没有。
* **找右边界时**：发现了 `target`，先记录为 **candidate**，然后**继续向右收缩** (`left = mid + 1`)，看看右边还有没有。

这种策略就像“捡麦穗”，手里拿着一个大的，但眼睛还得盯着前面有没有更大的。

---

## 3. C++ 代码实现

代码结构清晰化：将寻找左边界和右边界的逻辑拆分为两个函数。

```cpp
class Solution {
public:
    vector<int> searchRange(vector<int>& nums, int target) {
        if (nums.empty()) return {-1, -1};
        
        int left = searchLeft(nums, target);
        int right = searchRight(nums, target);
        
        return {left, right};
    }

private:
    // 寻找左边界（第一个等于 target 的位置）
    int searchLeft(vector<int>& nums, int target) {
        int left = 0;
        int right = nums.size() - 1;
        int candidate = -1; // 暂定解
        
        while (left <= right) {
            int mid = left + (right - left) / 2;
            if (nums[mid] == target) {
                candidate = mid;    // 找到了，先记下来
                right = mid - 1;    // 【关键】别停，向左收缩，找更靠前的
            } 
            else if (target < nums[mid]) {
                right = mid - 1;
            } 
            else {
                left = mid + 1;
            }
        }
        return candidate;
    }

    // 寻找右边界（最后一个等于 target 的位置）
    int searchRight(vector<int>& nums, int target) {
        int left = 0;
        int right = nums.size() - 1;
        int candidate = -1; // 暂定解
        
        while (left <= right) {
            int mid = left + (right - left) / 2;
            if (nums[mid] == target) {
                candidate = mid;    // 找到了，先记下来
                left = mid + 1;     // 【关键】别停，向右收缩，找更靠后的
            } 
            else if (target < nums[mid]) {
                right = mid - 1;
            } 
            else {
                left = mid + 1;
            }
        }
        return candidate;
    }
};
```

---

## 4. 总结
* **普通二分**：找到就停。
* **边界二分**：找到不停，记录 `candidate`，继续缩减区间。
* 这种“双二分”写法完全符合 $O(\log n)$ 的要求，即使在全重复数组中也能瞬间定位。